import main.Cell;
import main.State;
import org.junit.Assert;
import org.junit.Test;

public class CellTest {

    @Test
    public void changeState(){
//        Cell cell = new Cell();
//        Assert.assertEquals(State.DEAD,cell.getState());
//        cell.changeState();
//        Assert.assertEquals(State.ALIVE,cell.getState());

//        Cell cell1 = new Cell(State.ALIVE);
//        Assert.assertEquals(State.ALIVE,cell1.getState());

//        Cell cell2 = new Cell(State.DEAD);
//        Assert.assertEquals(State.DEAD,cell2.getState());
    }
}
